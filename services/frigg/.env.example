# .env files are auto-generated from environments/development.json on startup.
# To modify dev defaults, edit environments/development.json at the repo root.
# Run `pnpm env:init` or `pnpm dev` to regenerate.
