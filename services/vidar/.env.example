# .env files are auto-generated from manifest.json and the secrets cache on startup.
# To modify defaults, edit this service's manifest.json.
# For local overrides, create overrides.json at the repo root.
# Run `pnpm env:init` or `pnpm dev` to regenerate.
# Run `pnpm env:rotate` to regenerate all secrets.
